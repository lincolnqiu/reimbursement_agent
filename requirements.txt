pdfplumber>=0.10
openpyxl>=3.1
pillow>=10.0
# 如果需要 PDF 转图片：
# pdf2image>=1.17 
openai>=1.26.0
pydantic>=2.0
python-dotenv>=1.0.0
pdf2image>=1.17.0
# pdf2image 依赖 Poppler: 
# macOS: brew install poppler
# Debian/Ubuntu: sudo apt-get install poppler-utils
# Windows: 下载 Poppler binaries 并添加到 PATH 